# Development setup for Linux.
#
buildDir=build

rm -rf $buildDir
mkdir $buildDir
cd $buildDir

# Generation of tests and examples is enabled.
cmake \
    -DENABLE_TESTS=ON \
    -DENABLE_EXAMPLES=ON \
    ..
